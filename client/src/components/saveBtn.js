import { cloneElement } from "react";

const searchButton = search.querySelector('button')

document.addEventListener('click', () => {
    let data = {
        foodList
    }

    localStorage.setItem('user-saves', JSON.stringify(data))


})

    
   